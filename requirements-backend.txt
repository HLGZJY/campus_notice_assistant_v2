# 后端运行时依赖（给生产/运行镜像使用）
# 仅包含后端运行所需的最小包；引擎/开发依赖仍保留在 requirements.txt
fastapi>=0.95
uvicorn[standard]>=0.22
python-multipart>=0.0.6
pydantic>=2.8
requests>=2.31
PyYAML>=6.0
apscheduler>=3.10
openai>=2.45.0
beautifulsoup4>=4.12.2
# Engine deps to enable full extractor/crawler/agents behavior (names match requirements.txt)
# jieba 为 newspaper4k 中文分词必需（不含则中文页面提取失败）
jieba>=0.42
openai-agents>=0.19,<0.20
newspaper4k>=0.9.6

# 可选（测试/小工具）
httpx>=0.27
