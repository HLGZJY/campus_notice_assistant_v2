# 云端版专用构建环境依赖（PyInstaller 打包用）
# 原则：只装后端运行真正需要的包，避免把无关大件（paddle/cv2/playwright/spacy 等）打进产物。
# ⚠️ 不要在此环境中安装 streamlit / sentence-transformers / torch —— 云端版用百炼 embedding，不需要。
# 完整版（--flavor full）请用项目 .venv 或另建含 sentence-transformers 的环境。

# ---- 后端运行时（同 requirements-backend.txt）----
fastapi>=0.95
uvicorn[standard]>=0.22
python-multipart>=0.0.6
pydantic>=2.8
requests>=2.31
PyYAML>=6.0
apscheduler>=3.10
openai>=2.45.0
beautifulsoup4>=4.12.2
jieba>=0.42
openai-agents>=0.19,<0.20
newspaper4k>=0.9.6
httpx>=0.27

# ---- 向量检索 + RAG（云端版不含本地 embedding）----
chromadb>=0.6
langchain>=0.3
langchain-chroma>=0.2
langchain_community>=0.3
langchain_openai>=0.3
langchain_text_splitters>=0.3
rank_bm25>=0.2

# ---- 提取/配置辅助 ----
dateparser>=1.2
python-dotenv>=1.0
lxml>=5.0

# ---- 打包工具 ----
pyinstaller>=6.0
