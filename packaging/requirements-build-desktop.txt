# 桌面版（--flavor desktop）构建环境追加依赖（B00.T1）
# 定位：在 packaging/venv-build 共享构建环境之上追加的「壳层」依赖，
#       后端运行时依赖见 requirements-build-cloud.txt（桌面 flavor 复用同一后端，零改动）。
# 原则：壳层只引最小依赖（pywebview + pystray），不引入自带 Chromium 的重量级方案。
# ⚠️ Pillow 为 pystray 必需依赖，已随 newspaper4k 存在于 venv-build（实测 pillow==12.3.0），无需重复安装。
#
# 安装方式（B00 已执行）：
#   packaging/venv-build/Scripts/pip install -r packaging/requirements-build-desktop.txt

# ---- 窗口壳：系统 WebView2 承载（复用系统内核，零体积增量）----
pywebview>=5.0

# ---- 托盘：pystray 菜单与气球通知（依赖 Pillow）----
pystray>=0.19
