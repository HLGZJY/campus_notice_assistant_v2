# 校园通知智能助手 · 原生桌面版项目规划（PLAN.md）

> 状态：规划中（待评审） ｜ 版本：0.1 ｜ 更新日期：2026-08-24
> 关联：在线版（cloud）已发布 release（PyInstaller + Inno Setup + GitHub Releases）

---

## 0. 背景与目标

### 0.1 现状与痛点

在线版（cloud flavor）已经跑通完整链路：FastAPI 单进程后端 + APScheduler 常驻调度 +
Vue3/naive-ui 前端 + SQLite/Chroma 向量库，PyInstaller onedir 打包、Inno Setup 出安装包、
GitHub Releases 分发，`GET /update/check` 检查更新接口已存在。

当前唯一不满意的点是**展现形式**：

- 启动后残留一个终端窗口（控制台日志），观感不专业；
- 依赖默认浏览器打开页面，不是独立应用形态；
- 没有系统托盘、开机自启、后台驻留等"工具类应用"应有的能力。

### 0.2 目标

1. 将在线版改造成**独立桌面应用**：无终端窗口、无浏览器依赖、原生窗口体验；
2. 补齐工具类能力：开机自启、系统托盘、最小化到托盘、单实例、窗口状态记忆；
3. 建立智能调度：定时任务 + 系统空闲时自动执行重活 + 唤醒补偿；
4. 建立更新通知闭环：新版本 / 阶段性成果自动生成摘要并推送给用户；
5. 全流程以**资源占用最优**为优先目标（相对 Electron 路线）。

### 0.3 非目标（本期不做）

- 重写后端（Python 栈是既有资产，重写成本不可接受）；
- 重写前端为原生控件 UI（Vue3 界面已完善，保留为 webview 承载）；
- 移动端 / Web 端改动；
- 强制遥测与账号体系。

### 0.4 关键约束（决定技术路线）

- 后端重度依赖 Python 生态（FastAPI/APScheduler/Chroma/sentence-transformers/rank_bm25/jieba），
  且嵌入模型 bge-small-zh 本地加载（云端版走百炼 API）。→ **后端必须原样保留**；
- 前端已是纯静态产物 `frontend/dist`，可被任意 webview 承载；API 全部走
  `http://127.0.0.1:port`，含 **SSE 流式**（QA 回复、提取进度、任务状态）；
- PyInstaller + Inno Setup + GitHub Releases 打包链路已验证可用 → 优先复用；
- 数据布局当前为"exe 同级目录"（`data/`、`config/`、`.env`、`VERSION`），
  桌面化后需迁移到系统应用数据目录（见 §5 F5）。

---

## 1. 现状盘点

| 层 | 技术 | 现状 | 桌面化影响 |
|---|---|---|---|
| 后端 | FastAPI + uvicorn 单进程（127.0.0.1 随机端口 8000+） | run_app.py 启动 | 保留，改为内嵌线程启动 |
| 调度 | APScheduler（crawl/extract/daily/reminder/config-watch） | 随后端 lifespan 启动 | 保留，扩展空闲触发 |
| 存储 | SQLite（notices/tasks/todos/…）+ Chroma（向量） | exe 同级 `data/` | 需迁移到应用数据目录 |
| 嵌入 | 本地 bge-small-zh-v1.5 / 云端百炼 text-embedding-v4 | models/ + app.yaml | 桌面版建议默认云端，可降内存 |
| LLM | 百炼 DashScope（提取/QA/待办） | HTTP API | 无影响 |
| 前端 | Vue3 + naive-ui → frontend/dist | 静态产物 | webview 直接承载 |
| 打包 | PyInstaller onedir + Inno Setup（Windows） | packaging/build.py | 复用 + 桌面壳入口 |
| 更新 | GitHub Releases + `update/check` 接口 | 已有，仅"检查" | 补齐下载/安装/摘要/通知 |
| 路径 | utils/app_paths.py（冻结 = exe 同级） | 单点收敛 | 改造为平台应用数据目录 |

**结论**：技术路线唯一现实解是「**保留 Python 后端 + 保留 Vue 前端产物 + 换一个原生窗口壳**」。
壳的职责：起后端线程 → 打开无边框可定制的系统 WebView 窗口 → 托盘/自启/单实例/更新器。

---

## 2. 平台与技术选型（重点：资源占用对比）

### 2.1 候选方案总览

| 方案 | 壳实现语言 | 窗口技术 | 后端复用 | 维护生态 | 适配工作量 |
|---|---|---|---|---|---|
| **A. pywebview** | Python | 系统 WebView（Win=WebView2 / mac=WKWebView） | 同进程，零改动 | 中等（单库） | 最小 |
| **B. Tauri v2** | Rust | 系统 WebView（同上） | 需作为 sidecar 进程带 | 官方插件齐全（autostart/updater/single-instance） | 中（Rust 壳 + 双运行时） |
| **C. Electron** | Node/Chromium | 自带 Chromium | sidecar 进程 | 最成熟（electron-builder/updater） | 中，但体积/内存最大 |
| **D. PySide6 + QWebEngine** | Python/Qt | 自带 Chromium | 同进程 | 成熟（QSystemTrayIcon 等） | 中（QtWebEngine 与 Electron 同级重量） |
| E. Wails（Go） | Go | 系统 WebView | sidecar | 一般 | 中 |
| F. Flutter Desktop / .NET MAUI | Dart / C# | 自绘 / WinUI | 需重写 | — | **大（重写 UI）**，排除 |

### 2.2 资源占用对比（重点）

后端基线（与壳无关，两种形态）：
- 云端版（embedding 走 API）：空闲 ≈ 60~120 MB；
- 本地嵌入版（bge-small-zh 加载后）：空闲 ≈ 150~300 MB（模型常驻内存）。

壳层增量（方案差异的体现）：

| 方案 | 壳空闲内存增量 | 安装体积增量 | 启动到首屏 | 备注 |
|---|---|---|---|---|
| **A. pywebview** | **+20~50 MB** | **≈ 0（复用 PyInstaller 产物；WebView2/WKWebView 为系统共享）** | 1~2 s | 无自带浏览器内核，最省 |
| **B. Tauri v2** | +15~40 MB | +5~10 MB（壳本体，极小） | 1~2 s | 壳最轻，但 Python sidecar 另算 |
| **C. Electron** | +90~160 MB | +90~150 MB | 1~3 s | 多进程 Chromium 常驻，最重 |
| **D. PySide6+QWebEngine** | +120~250 MB | +200~300 MB | 1~3 s | 自带 Chromium，最重之一 |

> 数字为典型空闲值，随版本波动；**Phase 0 必须用真实构建实测**（§8 Phase 0 验收标准含资源数据）。

**结论**：壳层内存排序 Tauri ≈ pywebview << Electron ≈ QtWebEngine。
考虑到**总内存 = 后端基线 + 壳增量**，Electron/Qt 路线会让桌面版空闲内存直逼 300~500 MB，
违背"资源占用最优"目标，排除。

### 2.3 各方案优缺点与跨平台差异

| 维度 | pywebview（A） | Tauri v2（B） | Electron（C） |
|---|---|---|---|
| 内存/体积 | ✅ 最优 | ✅ 最优 | ❌ 最重 |
| 后端复用 | ✅ 同进程零改动 | ⚠️ Python 需打包成 sidecar（PyInstaller 不变，但多一层进程管理） | ⚠️ 同上 |
| 托盘/自启 | ⚠️ 无官方封装，需自研（pystray + winreg/LaunchAgent，工作量约 1 周） | ✅ 官方插件（autostart/single-instance） | ✅ electron-tray/auto-launch |
| 自动更新 | ⚠️ 自研（复用现有 GitHub Releases，工作量 1~2 周） | ✅ 官方 updater 插件（Tauri v2 内置） | ✅ electron-updater 最成熟 |
| 单实例 | 需自研（推荐 socket 锁，跨平台统一） | ✅ 官方插件 | ✅ 第三方成熟库 |
| 原生集成（全局快捷键/系统通知） | ⚠️ 部分需自己调系统 API | ✅ Rust 侧能力强 | ✅ Node 侧生态多 |
| 跨平台差异 | WebView2（Windows 需运行时，Win11 预装）/ WKWebView（macOS 内置） | 同左 | 双平台一致（自带内核） |
| 开发门槛 | 低（纯 Python） | 中（需 Rust 工具链 + 双语言联调） | 中（Node） |
| 已知风险 | WebView2 老 Win10 可能缺失；WKWebView 对 SSE/滚动有版本差异 | 打包/签名链路复杂（Rust + Python 两个运行时） | 体量大、杀软误报率高 |

### 2.4 推荐结论

- **P0（推荐）**：**pywebview + pystray + 自研轻量胶水层**（单实例锁、自启、更新器）。
  理由：后端零改动、PyInstaller 链路 100% 复用、资源最省、工期最短、现有 `update/check` 直接续接。
- **P1（备选）**：**Tauri v2 + Python sidecar**。若后续发现 pywebview 满足不了
  原生能力（如全局快捷键、原生通知、官方 updater），再迁移；架构上预留"壳可替换"边界（§3）。

**切换决策准则**：

| 触发条件 | 动作 |
|---|---|
| 需要全局快捷键 / 深度系统通知 / 官方 updater 插件 | 评估切换 Tauri |
| 安装包体积成为硬指标（< 30 MB） | 评估切换 Tauri |
| 只需要 Windows 且希望生态最省心 | 维持 pywebview，不升级 |
| Phase 0 实测壳内存 > 60 MB 或 WebView2 兼容问题严重 | 重新评估 |

---

## 3. 桌面版架构设计

### 3.1 进程模型（推荐：单进程内嵌）

```
┌─────────────────────────── 桌面应用进程（PyInstaller onedir） ───────────────────────────┐
│  主线程：webview 窗口（Windows: WebView2 / macOS: WKWebView）                              │
│    └─ 加载 http://127.0.0.1:<port>（复用现有 API + SSE，前端零改动）                      │
│  daemon 线程：uvicorn 单进程（api.main:app，含 scheduler lifespan）                        │
│  控制线程：托盘（pystray）、空闲检测轮询、更新检查、周报定时                                │
│  生命周期：health 轮询就绪 → 显示窗口 / 或最小化到托盘；关闭 → 拦截 → hide；退出 → 优雅停  │
└──────────────────────────────────────────────────────────────────────────────────────────┘
```

- 单进程内嵌 vs 双进程（壳 + 后端独立）：内嵌实现最简单、崩溃路径单一；uvicorn 在 daemon 线程，
  壳主线程负责窗口。后端若崩溃由壳监控 health 自动重启线程（看门狗）。
- 通信：前端 → 后端仍走 localhost HTTP（保留所有现有路由/SSE/任务轮询），**前端代码无需改动**；
  壳 → 后端通过新增的 `/api/v1/desktop/*` 控制面接口（托盘启停调度、更新触发等）。

### 3.2 安全设计（桌面化新增）

- 绑定 `127.0.0.1` + **随机端口**（现有逻辑保留），额外加**启动令牌**：
  每次启动生成随机 token，注入前端（webview 初始化时注入 JS 变量），API 侧校验
  `X-Desktop-Token` 头，防本机其他进程/网页调用本应用 API；
- 托盘"退出"走壳内直调（不依赖 API 权限）；
- `.env`（API Key）保持文件权限收紧，日志不落 key。

### 3.3 数据目录迁移（F5 前置设计）

- 新布局（桌面版默认）：Windows `%APPDATA%\CampusNoticeAssistant\`；macOS
  `~/Library/Application Support/CampusNoticeAssistant/`（含 data/、config/、.env、logs）；
- 老用户（在线版 exe 同级 data/）首启检测到旧目录 → **只读复制迁移向导**（复制不移动，
  原数据保留，迁移后标记），SQLite 与 Chroma 目录整体复制即可（文件级，无需导出）；
- `utils/app_paths.py` 改为三态：开发模式 / 在线版冻结（exe 同级）/ 桌面版（平台目录）。

---

## 4. 功能需求分解

优先级：P0 = 首版必须；P1 = 第二版；P2 = 可选/长期。
依赖列表示"此任务完成所需的前置任务"。

### F1 窗口与生命周期（P0，依赖：无）

| 任务 | 优先级 | 依赖 | 验收标准 |
|---|---|---|---|
| 单实例锁（socket 端口锁 + `/activate` 唤醒已有实例显示窗口） | P0 | — | 双开时第二实例自动退出并唤起主窗口 |
| 关闭按钮语义：拦截 closing → 最小化到托盘（托盘菜单"退出"才是真退出） | P0 | — | 点 X 窗口隐藏、进程常驻；托盘退出后进程消失 |
| 托盘图标 + 菜单：打开主界面 / 暂停恢复调度 / 检查更新 / 退出 | P0 | — | 菜单逐项可用 |
| 窗口位置/大小记忆（关闭时持久化，重启恢复） | P0 | — | 重启后几何恢复 |
| 启动参数 `--minimized`（自启用）、`--headless`（纯后台，仅托盘） | P1 | 自启 F2 | 参数生效 |
| 可选：无边框窗口 + 自定义标题栏 | P2 | — | 设计评审通过 |

### F2 开机自启（P0，依赖：F1）

| 任务 | 优先级 | 依赖 | 验收标准 |
|---|---|---|---|
| Windows：HKCU `...\CurrentVersion\Run` 写 `exe路径 --autostart --minimized` | P0 | F1 | 注册表项读写、注销重登后自动驻留托盘 |
| macOS：`~/Library/LaunchAgents` plist（RunAtLoad + LimitLoadToSessionType=Aqua） | P1 | F1 | 登录后自动启动到托盘 |
| 设置页开关：自启 on/off、启动方式（显示窗口/最小化到托盘）、延迟启动秒数 | P0 | — | 开关即时生效并持久化 |

### F3 智能调度（P1，依赖：F1、F2）

| 任务 | 优先级 | 依赖 | 验收标准 |
|---|---|---|---|
| 保留现有 APScheduler 定时 job（crawl/extract/daily/reminder），行为不变 | P0 | — | 现有测试全绿 |
| **空闲触发**：轮询系统空闲（Win `GetLastInputInfo` / mac `CGEventSourceSecondsSinceLastEventType`），空闲 > N 分钟（可配，默认 5）触发"重活队列"（深度抓取/批量提取/一致性检查）；用户活动恢复立即挂起 | P1 | — | 空闲 5 分钟自动跑重活；动鼠标 1 秒内停止新任务 |
| **电量感知**：笔记本电量 < 20% 且未充电 → 跳过重活；充电恢复 | P1 | — | 低电量不触发、充电恢复 |
| **唤醒补偿**：系统从睡眠/休眠恢复后 5 分钟内补跑错过的 job（扩展 APScheduler misfire） | P1 | — | 睡眠错过抓取，唤醒后自动补一轮 |
| 任务分级 gate：轻量任务（提醒扫描/体检）随时跑；重量任务（提取/深度抓取）仅空闲窗口 | P1 | — | 分级配置可调、默认值合理 |
| 调度状态可视化：现有 scheduler/status 接口 → 托盘菜单/设置页展示下一轮时间 | P2 | — | 界面显示 next_run |

### F4 更新与通知（P1，依赖：F1、F2）

| 任务 | 优先级 | 依赖 | 验收标准 |
|---|---|---|---|
| 自动检查更新：启动时 + 每 6 小时调 `update/check`（已有），新版本 → 通知 | P0 | — | 新版本发布后 6h 内收到通知 |
| **更新摘要生成**：取 release notes / git log（自上次版本 tag 起）→ LLM 生成"本次更新要点"摘要 | P1 | — | 摘要可读、< 200 字、失败降级为原文 |
| 应用内通知中心：未读徽标 + 通知列表页（更新摘要 / 周报 / 提醒），已读持久化 | P1 | — | 徽标/已读正确 |
| 托盘气泡推送（Windows balloon / macOS 通知），点击跳转 | P1 | — | 点击行为正确 |
| **阶段性成果周报**：每周日汇总（新增通知数 / 提取成功数 / QA 问答数 / 待办完成 / 体检摘要 / 调度失败数）→ LLM 生成摘要 → 通知中心；默认开启可关 | P2 | F4 摘要管线 | 周报数据与库中统计一致 |
| 更新执行（下载 → sha256 校验 → 提示 → 静默安装/重启替换） | P1 | §6 | 全流程走通 |

### F5 数据与存储（P0，依赖：无）

| 任务 | 优先级 | 依赖 | 验收标准 |
|---|---|---|---|
| 应用数据目录迁移（§3.3） | P0 | — | 新装/迁移两路径均正确 |
| 老版本数据迁移向导（检测 → 只读复制 → 校验 → 标记） | P0 | — | 迁移后功能一致，原数据保留 |
| 每周自动备份 `data/` → 保留最近 3 份，可配 | P1 | F5 目录 | 备份可恢复 |
| SQLite 并发确认（现有单写者模型不变；桌面版仍单进程，无新风险） | P0 | — | 现有并发测试通过 |

### F6 稳定与安全（P0，依赖：F1）

| 任务 | 优先级 | 依赖 | 验收标准 |
|---|---|---|---|
| 统一日志：data/logs 滚动（现有 RotatingFileHandler 复用）+ 启动写"排障信息"页（版本/端口/健康状态） | P0 | — | 异常时可一键导出日志 |
| 看门狗：health 探测失败自动重启后端线程，连续失败 3 次弹窗提示 | P0 | — | 注入崩溃可恢复 |
| localhost 启动令牌（§3.2） | P0 | — | 无令牌请求被拒 |
| 崩溃兜底：未捕获异常 → 弹窗（含日志导出按钮），不静默退出 | P1 | — | 模拟崩溃路径 |
| 杀软误报预案：签名证书 + 误报申诉说明文档 | P1 | §6 签名 | 文档就绪 |

### F7 前端适配（P1，依赖：F1）

| 任务 | 优先级 | 依赖 | 验收标准 |
|---|---|---|---|
| 外链（通知原文）改为系统浏览器打开（webview 拦截新窗口事件） | P1 | — | 点击外链走默认浏览器 |
| 下载/导出改用系统保存对话框 | P1 | — | 保存路径可选 |
| SSE 在 WebView2/WKWebView 下验证（Phase 0 已验，此处落代码） | P0 | — | 流式 QA/进度正常 |
| 剪贴板/打印等浏览器特性核对清单 | P2 | — | 清单核对完成 |
| 高分屏/DPI 缩放适配检查 | P1 | — | 125%/150% 缩放下布局正常 |

---

## 5. 平台适配

### 5.1 Windows

- **WebView2 Runtime**：Win11 预装 evergreen；Win10 老版本需检测——Inno Setup 安装时检测
  缺失则静默安装 Evergreen Bootstrapper（或 bundle 固定版）；
- **打包**：继续 Inno Setup；改 per-user 安装（免管理员），快捷方式（桌面/开始菜单）+ 卸载清理注册表/自启项/数据目录（可选保留数据）；
- **签名**：Authenticode 证书（EV/OV）→ 消除 SmartScreen 拦截；无证书期用"下载次数 + 说明页"过渡；
- 自启走 HKCU Run（§F2），托盘走 pystray，单实例走 socket 锁（§F1）。

### 5.2 macOS

- **必须在 macOS 上构建**（无跨平台编译）→ GitHub Actions `macos-14/15` runner（§5.3）；
- **签名 + 公证（必需）**：Developer ID Application 证书签名 → `notarytool` 公证 →
  `stapler` 装订；否则 Gatekeeper 拦截分发；
- 打包：PyInstaller onedir → 拖入 `/Applications` 的 DMG（`hdiutil`/create-dmg）；
- WKWebView 内置，无额外运行时；自启走 LaunchAgent（§F2）；
- 备注：macOS 上 Chroma/sentence-transformers 依赖需在 CI 同架构编译验证（arm64/x86_64）。

### 5.3 CI（GitHub Actions）

- 矩阵：`windows-latest` + `macos-latest`，步骤：构建前端 → PyInstaller → 冒烟测试
  （启动 → health 通过 → 关闭）→ 打安装包 → 上传 Release 工件；
- 签名材料存 CI secrets（Windows 证书 pfx / macOS 证书 + notary 密钥）；
- 产物命名规范：`校园通知助手-桌面版-vX.Y.Z-windows-setup.exe` / `-macos.dmg`。

---

## 6. 打包分发与自动更新（补充遗漏项）

### 6.1 更新机制选型

- **全量替换**（推荐首版）：下载新安装包 → 校验 → 用户确认 → 静默安装（Inno `/SILENT`）
  或提示手动运行。实现简单可靠，安装包约 50~100 MB，可接受；
- 差分更新（仅当安装包 > 200 MB 再考虑，本期不做）；
- 更新服务器：**继续用 GitHub Releases**（update.repo 已配）+ `download_prefix` 国内镜像切换位
  （已有字段）。新增 `latest.json` 清单：版本号 / sha256 / 下载 URL / 变更摘要 / 强制标记。

### 6.2 更新流程

```
启动/每6h → 调 update/check → 有新版本？
  ├─ 是 → 拉 latest.json → 下载安装包（失败重试 2 次）→ sha256 校验 → 失败回滚丢弃
  │        → LLM 生成摘要（F4）→ 通知中心 + 托盘气泡 → 用户确认 → 静默安装/重启替换
  └─ 否 → 静默
```

### 6.3 分发与灰度

- 通道：`stable`（默认）/ `beta`（可配 app.yaml `update.channel`）；
- 灰度：latest.json 支持 `rollout` 百分比（首版可先全量，量大后再加）；
- 版本策略：semver；`VERSION` 文件已是唯一版本源（build.py 已读）。

---

## 7. 你可能遗漏、但必须考虑的清单（汇总）

1. **代码签名与信任链**：Windows SmartScreen（未签名 = 拦截告警）、macOS 公证（不公证 = 无法分发）；
2. **自动更新机制本身**：检测/下载/校验/安装/回滚/灰度/镜像，不是"检查一下版本号"；
3. **数据目录迁移与备份**：老安装的 data/ 要平滑迁到平台目录，失败可回退；
4. **单实例与端口冲突**：多开会双写 SQLite → 损坏；随机端口 + socket 锁必须配套；
5. **localhost 安全**：绑定 127.0.0.1 不等于安全，需启动令牌防本机越权调用；
6. **崩溃诊断**：桌面版无终端，用户机器上拿不到日志 = 无法排障 → 内置日志导出/看门狗；
7. **浏览器特性适配**：外链、下载、剪贴板、打印在 webview 中的行为与浏览器不同；
8. **杀软误报**：PyInstaller 打包无签名程序极易被 Windows Defender 等误报；
9. **资源占用优化**：本地嵌入模型常驻是内存大头 → 桌面版默认云端嵌入，本地模式懒加载/空闲释放；
10. **per-user 安装与多账户**：避免管理员权限安装，数据按用户隔离；
11. **隐私与许可证**：遥测默认关闭；项目许可（如 MIT）与第三方组件清单；
12. **中文路径 / DPI / 高分屏**：安装路径含中文时 PyInstaller/Chroma 路径兼容性需验证；
13. **LLM 摘要成本**：更新摘要/周报是额外 token 开销，需节流（频率、缓存、可关）；
14. **macOS 硬件/CI 依赖**：无 Mac 无法本地构建 → 尽早确认 CI 或延期 macOS。

---

## 8. 分阶段实施计划与里程碑

### 8.1 阶段总览（相对周次，不含绝对日期）

| 阶段 | 名称 | 周次 | 关键交付 | 里程碑 |
|---|---|---|---|---|
| P0 | 可行性验证 | W1 | 选型定案 + 资源实测数据 | **M1** |
| P1 | 桌面壳 MVP（Windows） | W2~W4 | 单实例/托盘/最小化/窗口记忆/看门狗 | **M2** |
| P2 | 工具能力 | W4~W5 | 开机自启 + 智能调度（空闲触发/唤醒补偿） | **M3** |
| P3 | 更新与通知 | W6~W7 | 自动更新闭环 + 摘要 + 通知中心 | **M4** |
| P4 | 数据迁移与安全加固 | W7~W8 | 数据目录迁移 + 启动令牌 + 备份 | **M5** |
| P5 | macOS 与分发 | W8~W11 | CI 矩阵 + 签名/公证 + DMG + 灰度 | **M6** |
| P6 | 打磨与发布 | W11+ | 内存优化、崩溃上报、正式 release | **M7** |

### 8.2 依赖关系

```
P0 可行性验证
 └─▶ P1 桌面壳 MVP（Windows）
      ├─▶ P2 工具能力（自启+调度）
      │     └─▶ P3 更新与通知（复用 P2 的托盘/通知通道）
      ├─▶ P4 数据迁移与安全（可与 P2/P3 并行）
      └─▶ P5 macOS 与分发（依赖 P1~P4 验收 + CI 就绪）
            └─▶ P6 打磨发布
```

### 8.3 里程碑定义

| 里程碑 | 达成标准 | 验收方法 |
|---|---|---|
| **M1 选型定案** | webview 方案在 Win/mac 跑通现有前端 + SSE 正常；实测壳内存 < 60 MB | 资源实测报告 + 冒烟截图 |
| **M2 Windows MVP 内测** | 托盘/单实例/关闭最小化/窗口记忆/健康启动全部可用；后端行为与在线版一致 | 内测清单逐项勾验 + 现有测试全绿 |
| **M3 工具能力** | 自启、空闲触发（5 分钟空闲自动深度抓取）、唤醒补偿可用 | 场景化验证（空闲→触发、睡眠→唤醒） |
| **M4 更新闭环** | 发布新 tag → 6h 内收到摘要通知 → 一键更新成功 | 灰度小范围实测一次完整升级 |
| **M5 数据迁移** | 老安装 data/ 一键迁移、原数据保留、功能一致 | 迁移前后数据比对 |
| **M6 双平台分发** | Win/macOS 安装包 + 签名 + 自动更新灰度 | CI 产物 + 双平台冒烟 |
| **M7 正式发布** | 稳定运行 2 周无 P0 缺陷；内存达标 | 发布 check-list |

### 8.4 任务矩阵（WBS，含优先级与依赖）

| # | 任务 | 优先级 | 阶段 | 依赖 | 归属模块 |
|---|---|---|---|---|---|
| 1 | WebView 承载现有前端 + SSE 验证 | P0 | P0 | — | 壳 |
| 2 | 资源占用实测（壳/后端/嵌入模型三态） | P0 | P0 | 1 | 壳 |
| 3 | 选型评审定案 | P0 | P0 | 2 | 规划 |
| 4 | 壳进程模型 + app_paths 三态改造 | P0 | P1 | 3 | 壳/存储 |
| 5 | 单实例 socket 锁 + activate 唤醒 | P0 | P1 | 4 | 壳 |
| 6 | 托盘（pystray）菜单 + 关闭最小化 | P0 | P1 | 4 | 壳 |
| 7 | 窗口几何记忆 + 启动参数 | P0 | P1 | 5,6 | 壳 |
| 8 | 看门狗（health 探测 + 线程重启） | P0 | P1 | 4 | 壳 |
| 9 | 开机自启（Win HKCU / mac LaunchAgent）+ 设置开关 | P0 | P2 | 6 | 壳 |
| 10 | 空闲检测 + 重活 gate + 电量感知 | P1 | P2 | 9 | 调度 |
| 11 | 唤醒补偿 | P1 | P2 | 10 | 调度 |
| 12 | 更新检查定时化 + latest.json 清单 | P0 | P3 | 6 | 更新 |
| 13 | 更新摘要 LLM 管线 + 通知中心 | P1 | P3 | 12 | 更新/通知 |
| 14 | 下载/校验/静默安装 | P1 | P3 | 12 | 更新 |
| 15 | 周报（阶段性成果摘要） | P2 | P3 | 13 | 通知 |
| 16 | 数据目录迁移向导 | P0 | P4 | 4 | 存储 |
| 17 | 启动令牌 + API 侧校验 | P0 | P4 | 4 | 安全 |
| 18 | 自动备份 | P1 | P4 | 16 | 存储 |
| 19 | 前端适配（外链/下载/DPI） | P1 | P4 | 4 | 前端 |
| 20 | Windows 签名 + Inno 更新（per-user/WebView2 检测） | P1 | P5 | 8 | 分发 |
| 21 | macOS 构建/签名/公证/DMG | P1 | P5 | 4 | 分发 |
| 22 | CI 矩阵 + 冒烟 + 灰度 | P1 | P5 | 20,21 | 分发 |
| 23 | 内存优化（嵌入模型懒加载/空闲释放） | P2 | P6 | 2 | 后端 |
| 24 | 崩溃上报/日志导出 | P2 | P6 | 8 | 稳定 |
| 25 | 正式 release + 文档 | P0 | P6 | 22 | 规划 |

---

## 9. 风险与对策

| 风险 | 概率 | 影响 | 对策 |
|---|---|---|---|
| WebView2 在老 Win10 缺失 | 中 | 高 | Inno 安装时检测并静默安装 Evergreen Bootstrapper |
| WKWebView 渲染/SSE 差异 | 中 | 中 | Phase 0 预验；前端适配清单；冒烟用例 |
| PyInstaller 产物被杀软误报 | 高 | 中 | 签名证书 + 误报申诉；文档话术 |
| 空闲检测误判（合盖/睡眠/虚拟机） | 中 | 中 | 唤醒补偿 + 电量感知 + 可配空闲时长 |
| macOS 公证流程阻塞发布节奏 | 中 | 中 | CI 缓存签名材料；公证放非关键路径 |
| 数据迁移中途失败 | 低 | 高 | 只读复制 + 校验 + 原数据保留 + 失败可重试 |
| LLM 摘要 token 成本不可控 | 中 | 低 | 频率节流、缓存、可关；失败降级原文 |
| 双开/端口冲突损坏 SQLite | 低 | 高 | socket 单实例锁（首启绑定即加锁） |
| 项目无 macOS 硬件 | 中 | 中 | GitHub Actions macos runner 兜底 |

---

## 10. 开放问题（决策点，评审时确认）

1. **更新下载**：GitHub 直连还是国内镜像？（`download_prefix` 字段已预留，默认留空 = 直连）
2. **macOS 优先级**：本期是否必须？（无 Mac 硬件时用 CI 构建，但本地联调受限）
3. **周报（阶段性成果）**：默认开启还是默认关闭？（涉及每周固定 LLM 调用）
4. **无边框自定义标题栏**：首版做不做？（WebView2 拖拽/双击最大化有兼容坑，建议 P2 再说）
5. **浏览器模式兜底**：桌面版是否保留"浏览器打开"模式作为故障降级？（建议保留一个 `--browser` 参数）
6. **嵌入模型默认形态**：桌面版默认云端嵌入（省内存）还是本地（离线可用）？（建议默认云端，设置可切）
7. **自动更新确认方式**：静默安装 vs 用户确认？（建议首版"用户确认"，量大后再自动）
