# ---------- 运行时数据 ----------
data/notices.db
data/chroma/
data/logs/
data/health/
data/eval/
data/*.db
data/*.db-*
data/runtime.json
# B10.T5：WebView2 user-data（localStorage/缓存），固定 storage_path 目录，不得入库
data/webview/
storage/*.db
storage/*.db-*
*.tmp
*.bak
models/

# ---------- 密钥与环境 ----------
.env

# ---------- Python ----------
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# ---------- 前端 ----------
frontend/node_modules/
frontend/dist/
frontend/.vite/

# ---------- 编辑器/系统 ----------
.vscode/
.idea/
.DS_Store
Thumbs.db

docs-local/assest

# ---------- 批次开发过程产物（不入库） ----------
# 验收记录、回归基线、下一轮会话启动 prompt 等均属本地过程文件
docs-local/acceptance/

# ---------- 其他 ----------
config/app.yaml
config/schools/scuec.yaml
# 本机个性化配置已 git rm --cached 取消跟踪（默认配置保留在 config/scuec.yaml）

# ---------- WorkBuddy 工作记忆（不入库） ----------
.workbuddy/

# ---------- benchmark 临时输出 ----------
data/benchmark_*.json

# ---------- 单测临时库（test_vectorstore_singleton.py 等） ----------
data/test_vs_singleton_*

# ---------- 批次 demo / 实测脚本（计划文档约定不入库） ----------
scratch/

# ---------- 运行/调试产物（不入库） ----------
p.out
probe_sse.*
vite.log
server.log
_bwc_tztg.html
_test_adopt.py
_verify_history_log.py
data/scan_source_catalog_*

# ---------- 打包产物（packaging/build.py 生成，不入库） ----------
packaging/dist-*/
packaging/build-*/
packaging/out/
packaging/version.ini
packaging/__pycache__/
# 专用构建 venv（云端版打包）
packaging/venv-build/
# 构建排障残留（沙箱 DLL 删除拦截导致 rm 失败，整体改名隔离，勿入库）
packaging/.dist-cloud-trash/
# 构建过程日志
packaging/*.log
