# ---------- 运行时数据 ----------
data/notices.db
data/chroma/
data/logs/
data/health/
data/eval/
data/*.db
data/*.db-*
storage/*.db
storage/*.db-*
*.tmp
*.bak
models/

# ---------- 密钥与环境 ----------
.env

# ---------- Python ----------
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# ---------- 前端 ----------
frontend/node_modules/
frontend/dist/
frontend/.vite/

# ---------- 编辑器/系统 ----------
.vscode/
.idea/
.DS_Store
Thumbs.db

docs-local/assest

# ---------- 其他 ----------
config/app.yaml
config/schools/scuec.yaml
# 本机个性化配置已 git rm --cached 取消跟踪（默认配置保留在 config/scuec.yaml）

# ---------- WorkBuddy 工作记忆（不入库） ----------
.workbuddy/

# ---------- benchmark 临时输出 ----------
data/benchmark_*.json

# ---------- 运行/调试产物（不入库） ----------
p.out
probe_sse.*
vite.log
_bwc_tztg.html
_test_adopt.py
_verify_history_log.py
data/scan_source_catalog_*

# ---------- 打包产物（packaging/build.py 生成，不入库） ----------
packaging/dist-*/
packaging/build-*/
packaging/out/
packaging/version.ini
packaging/__pycache__/
# 专用构建 venv（云端版打包）
packaging/venv-build/
# 构建排障残留（沙箱 DLL 删除拦截导致 rm 失败，整体改名隔离，勿入库）
packaging/.dist-cloud-trash/
# 构建过程日志
packaging/*.log
