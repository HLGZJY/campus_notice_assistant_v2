# 阶段 7（Vue 3 + Vite + Naive UI）进度标记

> 记录时间：2026-08-12  
> 当前分支：`main`  
> 状态：脚手架就绪，核心源码待写。

---

## 已确定决策

- 埋点端点：选 **A**，后端新增 `POST /api/v1/events`。
- 工具链：从 Vite 8/TS6/vue-tsc3 降级为 Node 20 兼容版本（Vite 5.4/TS 5.6/vue-tsc 2.2）。
- Naive UI 按需导入：用 `unplugin-vue-components` + `NaiveUiResolver`（不存在 `unplugin-naive-ui` 包）。

---

### 1 导出 OpenAPI 契约并生成 TS 类型-已完成

```powershell
# 在仓库根目录
$env:APP_ENV='test'; .\.venv\Scripts\python.exe -c "import json; from api.main import app; json.dump(app.openapi(), open('frontend/openapi.json','w',encoding='utf-8'), ensure_ascii=False, indent=2)"
cd frontend
npm run gen:api
```

### 2 配置 Vite-已完成

- `frontend/vite.config.ts`：
  - `vue()` + `Components({ resolvers: [NaiveUiResolver()] })`
  - `server.proxy`：`/api` → `http://127.0.0.1:8000`
- `frontend/tsconfig.json` / `tsconfig.app.json`：按需调整 `paths` 等。

### 3 核心层源码-已完成

| 文件                                            | 内容                                                      |
| ----------------------------------------------- | --------------------------------------------------------- |
| `src/api/endpoints.ts`                          | 所有 `/api/v1` 端点常量                                   |
| `src/api/http.ts`                               | 基于 `fetch` 的薄封装、统一错误                           |
| `src/api/tasks.ts`                              | 轮询 `GET /tasks/{id}` 直到完成/失败                      |
| `src/api/events.ts`                             | `trackEvent`（fire-and-forget 吞错）                      |
| `src/router/index.ts`                           | 6 路由 + 路由守卫发送 `page_view` 埋点                    |
| `src/main.ts`                                   | naive-ui message、pinia、router                           |
| `src/App.vue`                                   | `NLayout` 侧栏 + `RouterView` + 全局提醒红点              |
| `src/stores/*.ts`                               | notices / todos / reminders / subscriptions / config / qa |
| `src/composables/useAsync.ts`、`useTaskPoll.ts` | 异步状态 + 任务轮询                                       |

### 4 视图与组件

| 路由             | 视图文件                          | 关键功能                      |
| ---------------- | --------------------------------- | ----------------------------- |
| `/`              | `src/views/HomeView.vue`          | 数据概览、提醒红点、统计卡    |
| `/notices`       | `src/views/NoticesView.vue`       | 列表/筛选/详情、抓取/提取任务 |
| `/todos`         | `src/views/TodosView.vue`         | 待办列表、状态变更、生成待办  |
| `/qa`            | `src/views/QaView.vue`            | SSE 流式问答                  |
| `/config`        | `src/views/ConfigView.vue`        | 模型/供应商/数据源配置        |
| `/subscriptions` | `src/views/SubscriptionsView.vue` | 两步式订阅预览 → 确认         |
| `/market`        | `src/views/MarketView.vue`        | 假服务按钮 + 埋点             |

### 5 后端静态挂载

`api/main.py` 启用 `StaticFiles` 挂载 `frontend/dist`，加 SPA fallback（所有非 API 路由返回 `index.html`）。

### 6 验收

```powershell
cd frontend
npm run build
# 后端启动后手测：浏览/待办/问答流式/订阅两步式/配置保存/市场埋点
```

## 7. commit 目标

`feat(web): Vue3+Naive UI 脚手架 + 6 页面平移，openapi-typescript 契约对齐`

---

## 8. Phase 8（收尾） — 已启动

- 已在后端启用对 frontend/dist 的静态挂载及 SPA fallback（见 `api/main.py`）。
- 后续工作（优先级）：
  1. 更新 README，记录后端/前端启动与部署步骤（必做）
  2. 生成 Dockerfile（可选，便于本地集成测试）
  3. 拆分与整理依赖文件：`requirements-backend.txt`（后端）与 `requirements.txt`（引擎）
  4. 将变更提交并打开 PR（代码审核）

状态：阶段7 编程实现已完成，进入阶段8 收尾与部署准备。