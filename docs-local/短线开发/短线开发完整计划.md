# 短线开发完整计划（4 周执行版）

> 本文件是《短线开发定稿.md》的执行层：定稿回答"为什么做、做什么、何时换技术"，
> 本文件回答"具体怎么做、每周做哪些任务、验收什么、涉及哪些现有代码"。
> 阅读顺序：定稿 → 本文件 → 相关周验收清单。本文件不重复定稿的战略内容（考量排序、技术选型、门控、里程碑、风险），只做任务拆解与落地。
>
> 粒度约定：任务描述级（写清楚"做什么、改哪个模块、验收什么命令"），不写 DDL 与具体实现细节，避免文档工程陷阱。
> 时间约定：15-20h/周，每周约 4 个模块；所有模块的"验收信号"均为可运行的命令或可演示的操作，不是"感觉跑通了"。

---

## 1. 现状基线盘点（执行前提）

MVP（M1–M6）已全部完成，实测数据与代码库现状如下，所有 4 周任务都建立在这个基线上：

### 1.1 已完成能力

| 里程碑 | 能力           | 关键文件                                  | 实测数据                                           |
| ------ | -------------- | ----------------------------------------- | -------------------------------------------------- |
| M1     | 抓取 + 存储    | `crawler/web_crawler.py`、`storage/db.py` | scuec 6 个数据源已验证，URL UNIQUE 去重            |
| M2     | 结构化提取     | `core/extractor.py`                       | 黄金集 6 条 24/24 满分，校验失败重试（≤2 次）      |
| M3     | 待办生成       | `core/todo.py`                            | 按需生成，重复点击先删旧再插                       |
| M4     | RAG 问答       | `core/qa.py`、`storage/vectorstore.py`    | 27 条通知 → 110 chunks，引用从检索元数据确定性导出 |
| M5     | Streamlit 界面 | `app.py`、`pages/` 4 页                   | 通知浏览 / 待办 / 问答 / 系统配置                  |
| M6     | 配置体系       | `config/store.py`、`config/schema.py`     | 任务级模型配置、供应商注册表、三层 fallback        |

### 1.2 空白区（与 W1–W4 的映射）

| 空白                 | 现状证据                                                           | 对应周  |
| -------------------- | ------------------------------------------------------------------ | ------- |
| 无调度器             | `config/app.yaml` 里 `crawl.interval_minutes: 60` 没有任何代码消费 | W1      |
| 无内容变更检测       | URL UNIQUE 只防重复插入，同 URL 正文更新无法感知                   | W1      |
| 无 token 计量        | `Runner.run()` 的 usage 结果被丢弃，`utils/llm.py` 无采集点        | W1      |
| 无 Tool Calling 演练 | 全项目无工具注册、无循环守卫                                       | W1 支线 |
| 无检索评测           | 没有测试集、没有召回率脚本                                         | W2      |
| 无过期处理           | 问答检索不过滤过期通知，Chroma 无 TTL                              | W2      |
| 无 RAG 污染防护      | 删通知的向量同步依赖手工"重建索引"                                 | W2      |
| 无订阅/提醒/用户体系 | 库中只有 notices / crawl_log / todos 三张表                        | W3      |
| 无埋点               | 页面交互零记录                                                     | W4      |

> 关键认识（写入本计划的纪律）：以上空白全部是"确定性工程"——调度、指纹、计量、评测、埋点，
> 均不需要 LLM。4 周内 LLM 只出现在既有的提取/待办/问答三处，不再新增 LLM 调用点。

---

## 2. 4 周执行计划

### W1 让系统活着：定时调度 + 增量闭环（≈18h，主线 14h + 支线 4h）

**周目标**：系统在无人干预下自动跑"抓取→提取→索引→待办"闭环；崩溃重启可续跑；每次 LLM 调用都留下账本。

#### 模块 1.1 调度器独立进程（≈5h）

- 子任务：
  1. 新增调度进程入口（如 `scheduler.py`，独立于 Streamlit 与 CLI 运行），基于 APScheduler
  2. 注册三个 job：定时抓取（消费 `crawl.interval_minutes`）、抓取完成后触发提取、每日定时过期清理 + 向量一致性检查
  3. job 失败不吞异常：写日志 + 记录失败计数，下一周期自动再跑
  4. 提供启动/停止方式（如 `python scheduler.py` + 日志文件），并说明 Windows 下后台运行的可行做法
- 涉及现有模块：`config/store.py`（读间隔）、`services/notice_service.py`（复用抓取/提取逻辑）、`storage/db.py`（crawl_log）
- 依赖：无（独立于其他模块，W1 最先做）
- 验收信号：
  - 配置 `interval_minutes` 改小（如 1 分钟）可观察自动抓取被触发
  - `kill` 调度进程后重启，运行状态可从日志/库中恢复，不重复抓取已抓 URL

#### 模块 1.2 内容指纹变更检测（≈3h）

- 子任务：
  1. `notices` 表增加内容指纹字段（正文哈希）
  2. 抓取时对已存在 URL 比较指纹：正文变化 → 更新 raw_content + 重置 status=raw + 标记 changed
  3. 被标记为 changed 的通知在下一轮提取中被处理，并同步更新向量索引（走既有"提取成功后增量更新索引"链路）
- 涉及现有模块：`storage/db.py`、`crawler/web_crawler.py`、`services/notice_service.py`、`storage/vectorstore.py`
- 依赖：无
- 验收信号：
  - 手工修改库里某条通知的正文并重新抓取，该条自动进入待提取状态，索引中对应 chunk 被更新
  - 正文未变的通知不触发任何提取/索引动作（日志可见 skipped）

storage/db.py:迁移列content_hash + total_changed、回填、compute_content_hash、get_notice_by_url 扩展、update_notice_content.insert_notice、 log_crawl
storage/models.py:NoticeRecord.content_hash、CrawlResult.total_changed
crawler/web crawler.py:已存在URL抓详情页比较指纹、变更更新、skipped 日志
services/notice_service.py + schedulerpy + Ul + check_db: changed 计数透出编写 test_content_fingerprint.py 验证脚本并运行验收

#### 模块 1.3 断点续跑（status 游标）（≈3h）

- 子任务：
  1. 核对并补齐"按 status 过滤待处理项"的游标语义：raw → 提取；changed/raw → 索引
  2. 模拟崩溃场景验证：提取进行中 kill 进程 → 重启 → 只处理未完成的，已完成的不再调用 LLM
  3. 与 1.4 联动：用 token 计量表证明"重启不重复计费"
- 涉及现有模块：`extract.py`、`index.py`、`services/notice_service.py`
- 依赖：模块 1.1、1.4（验证不重复计费需要计量表）
- 验收信号：
  - 两次运行后 `token_usage` 表中同一批通知只有一次提取计费记录
  - 未完成项（status=raw）在重启后继续被处理

storage/db.py: token_usage 表 + log_llm_usage + count_token_usage_by_task +get_notices_by_status 加 source 参数与稳定排序
core/extractor.py: extract_one 加 notice_id 参数，\_call 内采集 usage 落计量表extract.py: run_batch 支持注入 extractor + main 修复 --source/LIMIT 截断 bugservices/notice_service.py: extract_batch /\_extract_one_async 支持注入extractor+传notice_id
test_resume.py:新增崩溃续跑 + 不重复计费验收脚本
运行 python test_resume.py 验证全部断言通过

模块 1.3 已完成，python test_resume.py 26 项断言全部通过。

改动

storage/db.py：新增 token_usage 计量表（含 notice_id 归因列，为 1.4 铺路）+ log_llm_usage() / count_token_usage_by_task()；get_notices_by_status 加 source 参数且改 ORDER BY id 稳定游标。
core/extractor.py：extract_one 加 notice_id；\_call 在 Runner.run 返回后从 raw_responses.usage 汇总 input/output token 落计量表，成功/失败/重试均记账，计量失败不影响主流程。
extract.py：run_batch 支持注入 extractor（测试用）；main 把 --source 过滤前移到 SQL，修复 LIMIT 截断 bug。
services/notice_service.py：extract_batch / \_extract_one_async 支持注入 + 传 notice_id。
test_resume.py（新）：离线崩溃模拟验收。
验收信号对照

✅ 未完成项（status=raw）重启后继续被处理：A/B 两组 kill 后仅未完成项留 raw，重启全部非 raw。
✅ 已完成的不再调用 LLM：已完成通知全程调用计数恒为 1；仅被 kill 的 in-flight 项多调 1 次（at-least-once）。
✅ token_usage 同一批通知只有一次提取计费记录：两次运行后每条通知计费行数恰为 1，count_token_usage_by_task.calls 与总调用数一致。
真实计量路径也已单独冒烟验证（成功行记 token 数、失败行记 error、retry_count 区分重试）。注意 python extract.py --help 在未设 UTF-8 的 Windows 控制台会因中文 help 报编码错，这是改动前就存在的环境问题（$env:PYTHONIOENCODING=utf-8 即可）。

#### 模块 1.4 token 计量表（≈3h）

- 子任务：
  1. 新增计量表（记录：时间、任务类型、模型、input/output token、成功/失败、重试次数）
  2. 在 `utils/llm.py` 统一调用点采集 usage（提取/待办/问答三条链路自动覆盖，不用在每处重复埋点）
  3. 系统配置页增加"近 7 天 token 消耗"展示（按任务/模型分组）
- 涉及现有模块：`utils/llm.py`、`storage/db.py`、`pages/4_⚙️_系统配置.py`、`core/`（三 Agent）
- 依赖：无
- 验收信号：
  - 跑一次提取后，计量表出现对应记录，input/output 数与实际调用相符
  - 重试也记账（能区分首调与重试）
  - 配置页能展示汇总

Step 1:utils/llm.py 统—调用点 record_llm_usage + run_agent
Step 2:三 Agent 接入 run_agent (extractor/todo/qa)
Step 3: utils/embedding.py 埋点 task=embedding
Step 4: storage/db.py get_token_usage_summary
Step 5: services/usage service.py + 配置页新增 Token 用量 Tab
Step 6:新建 test_token_usage.py 并跑 test_resume.py 回归

总结
已实现（模块 1.4 token 计量表，提交 fb09584）

统一调用点 utils/llm.py：新增 run_agent()（提取/待办/问答三条 Agent 链路共用，成功从 raw_responses 累加 token、失败记 success=0 后上抛）和 record_llm_usage()（embedding 复用，全项目单写入路径）
三 Agent 接入：extractor.py 删掉内联埋点改用 run_agent；todo.py 重试循环传 attempt；qa.py 接入
embedding 埋点（按你的要求顺带做）：OpenAI-compatible 路径改直连 /embeddings 读 usage.prompt_tokens 记账；本地模型记 count-only
汇总查询 storage/db.py:get_token_usage_summary：近 N 天按任务×模型分组
配置页：新增第 6 个 Tab「📊 Token 用量」，含汇总 metric、分组表格、柱状图
测试：新增 test_token_usage.py（31 项离线验收）
功能验收结果

python test_token_usage.py → 31/31 全部通过：usage 数值与调用相符、重试 retry_count 区分首调/重试、失败记账、四条链路覆盖、汇总分组正确
python test_resume.py → 1.3 断点续跑回归全过（重启不重复计费不受影响）
所有改动模块 import 冒烟通过
一个验收信号仍需真实环境确认：规格里"跑一次提取后，计量表出现对应记录，input/output 数与实际调用相符"——离线测试用 mock usage 验证了逻辑正确，但真实 LLM 返回的 usage 数值需要你本地跑一次真实提取才能最终确认（需要 API key）。

真实提取验证通过 ✅

结果：python extract.py --limit 3 成功提取 3 条真实通知（数学建模培训报名 / 工程实践与创新能力大赛 / iCAN 数字经济挑战赛），全部 extracted，无重试无失败。

计量表记录与真实调用相符：

notice input output success retry
#180 1522 1470 1 0
#181 1593 2426 1 0
#182 1026 1272 1 0
每条提取恰好 1 条记录，task=extraction、model=qwen3.7-max，input/output 为 dashscope 真实返回数值
近 7 天汇总正确聚合：3 次调用，input 4141 / output 5168，失败 0，重试 0
至此 4 项验收信号全部核实：

提取后计量表出现对应记录、token 数与实际调用相符 ✅（真实调用验证）
重试也记账且能区分首调/重试 ✅（离线测试，真实场景首调即成功）
配置页能展示汇总 ✅（get_token_usage_summary 已验证，第 6 个 Tab 调用它）
提取/待办/问答三条链路自动覆盖 ✅（离线测试 + 本次真实提取）

#### 模块 1.5（支线，与主线并行）Tool Calling 循环演练（≈4h）

- 子任务：
  1. 在独立脚本中注册假工具 `query_mock_service(service_type)`（返回硬编码的打印店/跑腿电话），不接真实服务
  2. 强制最大迭代守卫（Max 5 轮），每轮打印 `tool_calls` 日志（调了什么工具、参数、返回）
  3. 实现 tool_call_id 去重：同一工具同参数只调一次，缓存上次输出并告知 LLM"结果一样，换别的办法"
  4. 验证"工具返回服务不可用"时 LLM 不瞎编，而是如实告知用户暂无结果
- 涉及现有模块：新建脚本（不污染主线代码）；参考 `core/qa.py` 的 Runner 用法
- 依赖：无
- 验收信号：
  - 手动构造"帮我找打印店"prompt，可观察每轮 tool_calls 日志回放
  - 构造工具失败场景，回答不出现编造的电话/价格
  - 构造重复调用场景，第二轮起被守卫拦截

模块 1.5 已完成。新增根级脚本 tool_call_drill.py，未触碰任何主线代码（git status 仅新增该脚本与 data/logs/ 下日志）。

实现要点：

假工具 query*mock_service(service_type)：@function_tool 注册，硬编码打印店/跑腿/文印店数据，不接真实服务（tool_call_drill.py:105）
最大迭代守卫：Runner.run(..., max_turns=5)，catch MaxTurnsExceeded 优雅收尾（tool_call_drill.py:230）
tool_call_id 去重：闭包内按本次 run 维度缓存，threading.Lock 防并行竞态，命中返回 duplicate_call 守卫消息（tool_call_drill.py:117）
反瞎编：instructions 约束只用工具数据；--scenario unavailable 验证如实告知
逐轮日志：DrillHooks（on_llm_start/on_tool_start/on_tool_end）打印 [轮次 N] → tool_call → 返回，并落盘 data/logs/tool_call_drill*<时间戳>.json

**W1 周末验收**：调度进程已运行 ≥24h 无干预；崩溃重启演练完成；计量表有数；Tool Calling 日志回放可演示。

---

### W2 检索质量基线：评测驱动优化（≈17h）

**周目标**：用 20 题测试集量化当前检索水平，产出"改进前后对比表"（负结果也算有效结果），并处理过期陷阱与 RAG 污染两个专项。

#### 模块 2.1 20 题测试集（≈3h）

- 子任务：
  1. 从现有 27 条通知构造 20 题，格式：问题、期望答案要点、期望命中的来源通知、类型标签
  2. 题型分布：常规语义题（≥8）、专有名词题（通知编号/课程名/竞赛名，≥4）、**过期陷阱题（5 条**：问"最近的 X"但唯一答案是过期通知）、污染用例（2 条：针对将被删除通知的问题）
  3. 每题配人工判定说明（什么算"命中"），落盘为 JSON，可单独复核
- 涉及现有模块：`data/` 下新建测试集文件；`evaluate_extraction.py` 的评估约定可参考
- 依赖：无
- 验收信号：测试集文件存在，每题有 type 标签，抽查 5 题判定说明清晰

完成。data/retrieval_testset.json 已落盘并校验通过。

验收对照

测试集文件存在 ✓
每题有 type 标签 ✓
抽查 5 题（Q01/Q10/Q14/Q19/Q20）判定说明清晰 ✓（上面已读 Q01 确认落盘正确）
结构：27 条语料（corpus）+ 统一判定约定（judge_conventions）+ 20 题。 题型分布：semantic 9 / proper_noun 4 / expired_trap 5 / pollution 2，全部达标。 一致性：20 题 id 唯一、每题有 judge、expected/must_not 均引用语料内 id、deletion_target 恰 2 条。

备注：Chroma 现仅含 1 条通知；模块 2.2 跑基线前需先对这 27 条语料 extract + index。

#### 模块 2.2 纯向量基线评测脚本（≈3h）

- 子任务：
  1. 评测脚本读取测试集，对每题跑 Chroma 检索，判定 Top5 是否包含期望来源
  2. 输出指标：Top5 召回率、MRR、按题型分组结果（重点看专有名词题与过期陷阱题的短板）
  3. 每次跑评测结果落盘（时间戳），为后续对比留档
- 涉及现有模块：`storage/vectorstore.py`（search 接口）、`core/qa.py`
- 依赖：模块 2.1
- 验收信号：基线表产出；能明确说出"哪类题召回差"

模块 2.2 开发完成。交付物与验收结果如下：

交付物
evaluate_retrieval.py — 纯向量检索基线评测脚本
读 data/retrieval_testset.json，每题跑 index.search(q, k)，按 judge_conventions 判定命中
指标：Hit@K / Recall@K / MRR@K，全体 + 按题型分组，每题含 rank、命中、污染标志
落盘（时间戳，供 2.3/2.4 对比）：data/eval/retrieval/<时间戳>\_results.json + \_report.md
--rebuild-corpus 用测试集 27 条语料重建索引（解决 index.py 默认只索引 extracted 导致索引缺失的问题）
pollution 题只评删前命中，标注第二阶段归 2.5；无 LLM 调用
基线结果（top_k=5，27 条语料 / 127 chunks）
题型 题数 Hit@5 MRR
语义理解 9 56% 0.352
专有名词 4 75% 0.562
过期陷阱 5 40% 0.267
污染用例 2 100% 0.750
全体 20 60% 0.412
短板结论
过期陷阱题最弱（40%）：Q14/Q16/Q17 全部失手——届次混淆（"最近"与"第十三届"等定位键）把 Top5 引向同赛事其他届次（205/181/182）。
语义题 Q01/Q04/Q05/Q06 召回差：列表完整性（Q01 期望 180/181/182 全落空）、"2026年"限定被 205/206（2025届）抢占。
专有名词 75%，仅 Q11（NECCS 缩写 ↔ 全称对应）失手。
对比口径已统一（语料即检索 universe），结果可复现：python evaluate_retrieval.py --rebuild-corpus。未提交 git。

#### 模块 2.3 过期处理实验（≈4h）

- 子任务：
  1. 实现三档策略并各跑一遍测试集：A 不过滤（基线）/ B 过期降权（时间衰减重排）/ C 过期排除（检索前过滤）
  2. 过期判定规则：以 `deadline` 为准，无 deadline 的按发布日 + 默认有效期兜底
  3. 输出三档对比表，含"过期陷阱题单独一组"的对比
- 涉及现有模块：`storage/vectorstore.py`（检索入口加过滤参数）、`core/qa.py`
- 依赖：模块 2.2
- 验收信号：对比表落盘；陷阱题召回有可量化结论（哪档策略最优）；负结果也接受

模块 2.3 实施完成。

改动
storage/vectorstore.py：search() 增加三档策略入口 + 过期判定辅助（deadline 优先、无 deadline 按 published_at+expire_days 兜底，默认读配置 90 天）。filter 走底层 Chroma $nin（绕过 langchain_chroma 的 where bug），decay 用 similarity_search_with_score 时间衰减重排不剔除。
evaluate_retrieval.py：evaluate_question/evaluate 加 search_kwargs 透传（向后兼容，基线逻辑不变）。
core/qa.py：QAAgent 透传 strategy/expire_days（默认 none，生产行为不变）。
evaluate_expired.py（新建）：三档对比实验，落盘 data/eval/expired/<ts>\_comparison.json|md。
实验结果（top_k=5, reference_date=2026-08-11, expire_days=90）
策略 全体 语义 专名 陷阱 污染
A 不过滤(基线) 60% 56% 75% 40% 100%
B 过期降权 45% 67% 25% 40% 0%
C 过期排除 15% 33% 0% 0% 0%
陷阱题单独一组：A/B 均 40%（A 命中 Q15/Q18，B 命中 Q14/Q16，MRR A=0.267>B=0.167），C 归零。

结论（量化，负结果）
陷阱题最优 = A（基线不过滤）；C 档把 21/27 条语料整批剔除，陷阱与历史类专名题全灭，不可用。
B 档语义题 +11pp（降权把 2025 届噪声挤出 Top5），但以专名 -50pp、污染 phase-1 归零为代价；默认强度结论对 decay_strength∈[0.02,0.2] 稳健（仅 0.005 时陷阱到 60%，代价不变）。
推荐：维持不过滤基线；B 的语义收益若后续要采纳，需容忍专名/污染回退（留给 2.4 混合检索对比时一并决策）。
验收
A 档与 2.2 基线完全一致（60%/40%/MRR 0.412）✓
--rebuild-corpus 复现结果一致 ✓
evaluate_retrieval.py 回归基线不变；QAAgent(strategy=...) 冒烟通过 ✓

#### 模块 2.4 混合检索 BM25 + RRF（≈5h）

- 子任务：
  1. 用 `rank_bm25` 实现 BM25 检索器（对同一批 chunk 建索引）
  2. 用 RRF（k=60）融合 BM25 与向量结果，注意是"排名倒数求和"而非分数加权
  3. 与纯向量基线跑同一测试集对比：MRR/Rec@k 提升 ≥10% 且无回归才保留，测不出差异就明确写"不上"
- 涉及现有模块：`storage/vectorstore.py` 旁新增混合检索模块；`core/qa.py` 检索部分可切换
- 依赖：模块 2.2、2.3（对比基准与过期策略需先定）
- 验收信号：对比表落盘；结论明确（上/不上，附数据理由）

模块 2.4 实施完成。

改动

- storage/hybrid.py（新）：BM25 + RRF 混合检索。BM25 语料从 Chroma collection.get() 直接拉取（与向量库同批 chunk，含 header 前缀，不重切分）；jieba 中文分词 + 英文/数字小写归一化；RRF k=60 按排名倒数求和融合两路（候选池 20），非分数加权；过期策略与向量路对齐（filter 剔过期 / decay 时间衰减）。
- core/qa.py：QAAgent 新增 search_mode="vector"/"hybrid"（默认 vector，生产行为不变）。
- evaluate_hybrid.py（新）：同一测试集跑纯向量 vs 混合，门控 #4 决策（MRR 与 Recall@K 相对提升均 ≥10% 且各题型无回归），落盘 data/eval/hybrid/。
- test_hybrid.py（新）：RRF 倒数求和、jieba 分词、BM25 中文召回、接口对齐 27 项断言全过。
- requirements.txt：新增 rank_bm25、jieba。

实验结果（top_k=5，RRF k=60，候选池 20，reference_date=2026-08-11）
| 组别 | 纯向量 Hit/Rec/MRR | 混合 Hit/Rec/MRR | ΔRec | ΔMRR |
| --- | --- | --- | --- | --- |
| 全体 | 60% / 60% / 0.412 | 85% / 88% / 0.792 | +47% | +92% |
| 语义理解 | 56% / 56% / 0.352 | 78% / 85% / 0.833 | +53% | +137% |
| 专有名词 | 75% / 75% / 0.562 | 75% / 75% / 0.750 | +0% | +33% |
| 过期陷阱 | 40% / 40% / 0.267 | 100% / 100% / 0.667 | +150% | +150% |
| 污染用例 | 100% / 100% / 0.750 | 100% / 100% / 1.000 | +0% | +33% |

结论：**上**。MRR +92%、Recall@5 +47%，各题型无回归（陷阱题 40%→100%，新增命中 Q04/Q05/Q14/Q16/Q17）；参数敏感性候选池 10/40、RRF k=20 下结论稳健（MRR +92~98% / Rec +47~61%）。按你拍板，core/qa.py 保留 search_mode 参数、默认仍 vector，需启用时显式传 search_mode="hybrid"。

#### 模块 2.5 RAG 污染专项（≈2h）

- 子任务：
  1. 复现：删除一条通知 → 问针对该通知的问题 → 观察幽灵结果
  2. 实现一致性校验脚本：对比 Chroma 与 SQLite 的通知 ID 集合，列出残留向量
  3. 确定并落地处理策略（删除通知时级联删向量 / 重建索引时自动清理残留），策略写入简要文档
- 涉及现有模块：`services/admin_service.py`（删除链路）、`storage/vectorstore.py`、`index.py`
- 依赖：无
- 验收信号：删除通知后检索不到；校验脚本可一键跑出"一致"结论

模块 2.5 RAG 污染专项已实施完成并全部验证通过。

改动清单
storage/db.py — 新增 get_all_notice_ids()（幽灵向量判定参照集 = SQLite 全量 ID）。
storage/vectorstore.py — remove_notice() 改为按 id 精确删除并返回实际删除数（杜绝"删了没删掉"）；新增共享核心 check_consistency()（检测/清理残留，ghosts_found/ghosts/ghosts_removed/missing）。
scheduler.py — \_check_vector_consistency() 复用共享核心，修复潜在 bug：旧实现用"可索引"子集作参照，会把大量 raw 状态通知的有效向量误当幽灵删除。
check_vector_consistency.py（新 CLI）— 一键跑"一致"结论，--fix 清理、--json 机器可读、退出码 0/1。
reproduce_pollution.py（新）— 沙箱（临时拷贝库+Chroma，不污染真实数据）走真实删除链路，对 Q19/Q20 两阶段判定。
docs/RAG-POLLUTION.md（新）— 问题定义、幽灵判定基准、检测工具、三层处理策略、验收方式。
验证结果
python check_vector_consistency.py → ✅ 向量一致（无残留），exit 0。
python reproduce_pollution.py → Q19/Q20 删前命中、删后无幽灵全部 ✅，沙箱内一致性校验 ✅，exit 0。
沙箱造鬼测试：注入 notice_id=99999 幽灵向量 → 报告模式检出；--fix 清理 1 chunk，复查后 consistent=True；scheduler.\_check_vector_consistency 复用正常。
真实数据（notices.db、data/chroma）全程未改动。

**W2 周末验收**：对比表齐全（基线 / 过期三档 / 混合检索）；污染专项有明确处理策略且可演示；门控 #4（混合检索）数据产出。

---

### W3 主动提醒 + 关键词订阅（≈17h）

**周目标**：全链路"通知发布 → 订阅命中 → 截止提醒 → 用户处理"可演示；提醒不依赖 Streamlit 进程存活。

#### 模块 3.1 订阅模型 + 规则引擎（≈5h）

- 子任务：
  1. 新增订阅表（订阅词、关注的通知类型、启停状态），先单用户，不引入登录
  2. 实现确定性匹配引擎：通知标题/摘要/类型 与 订阅词 精确匹配（子串），**不用 LLM**（符合"能用规则不用 AI"边界）
  3. 订阅管理页（Streamlit 4 号页或独立小页）：增删改订阅词
- 涉及现有模块：`storage/db.py`、`services/`（新增订阅服务）、`pages/`
- 依赖：无（W1 调度器有助于但非必须）
- 验收信号：新增订阅词后，抓取到含该词的通知被标记"命中订阅"；库中可查命中关系

模块 3.1 实施完成，验收脚本全绿。

改动清单

storage/db.py:202 行新增：subscriptions + notice*subscription_matches 两表（SCHEMA 自动建表）；订阅 CRUD、命中关系查询、级联清理（删除通知/来源/状态时同步清理命中）
services/subscription_service.py（新）：纯规则匹配引擎（大小写不敏感子串 + 类型过滤，不用 LLM）+ match_notice/match_all_notices 回填 + 订阅 CRUD/启停
crawler/web_crawler.py：插入/内容变更后自动匹配（web_crawler.py:14）
services/notice_service.py：提取成功后再匹配（补摘要/类型维度）
pages/5*🔔*订阅管理.py（新）：增删改、启停、命中数统计、全库重匹配
pages/1*📋\_通知浏览.py：🔔命中徽标 + "仅看命中订阅"筛选
app.py：快捷入口加订阅页
docs/DATA-MODEL.md：补两张表结构与 ER 关系
验收：python test_subscription.py → 9 组 42 项全部 PASS（含回填、类型过滤、启停清理、幂等、级联删除、修改订阅）；两个 Streamlit 页用 AppTest 无异常跑通，添加订阅表单实测落库。真实库已自动迁移出新表，测试订阅已清理。

#### 模块 3.2 截止提醒（≈7h）

- 子任务：
  1. 新增提醒表（关联通知/待办、截止时间、提醒档位、状态：待处理/已读/忽略）
  2. 每日扫描任务（挂到 W1 调度器）：对截止前 3 天与 1 天的待办/通知各生成一条提醒，幂等（同一天同一对象不重复生成）
  3. 首页红点 + 待办中心提醒区展示；已读/忽略操作
  4. 提醒链路不依赖 Streamlit：调度进程独立生成，UI 只是读
- 涉及现有模块：W1 调度器、`storage/db.py`、`services/todo_service.py`、`app.py` 与 `pages/2_✅_待办清单.py`
- 依赖：W1 模块 1.1（调度扫描）、W2 模块 2.3（过期策略可复用判断"已过期"）
- 验收信号：
  - 造一条截止在 3 天内的通知 → 扫描任务运行后产生提醒 → UI 出现红点
  - 同一天重复扫描不产生重复提醒
  - 杀掉 Streamlit 不影响提醒生成

模块 3.2 实施完成，test_reminder.py 全部通过（40 项），test_subscription.py 无回归，UI 页面 AppTest 无异常。

改动摘要：

storage/db.py：新增 reminders 表（notice*id/todo_id/due_at/tier/remind_on/status，幂等键 UNIQUE(notice_id, tier, remind_on)）+ insert/get/set/count/resolve/delete 函数；set_todo_status 在待办 done/skipped 时自动把待处理提醒置为已读；delete_notice/按来源/按状态删除均级联清提醒。
services/reminder_service.py（新）：scan_reminders() 每日扫描——通知路（有 deadline 的通知命中 3/1 天，有待办则挂 todo_id）+ 待办兜底路（通知无 deadline 但待办有 due_at 的数据异常场景），INSERT OR IGNORE 幂等；get_pending_reminders/get_reminder_stats/count_pending_reminders/mark_reminder。
scheduler.py：新增独立 reminder job（每日 03:00 cron，复用失败计数 + scheduler_log），--once 也跑，--no-reminder 可关闭，启动日志同步。
services/admin_service.py：delete_todo 级联删提醒。
app.py：首页红点徽标（🔔 待处理提醒 N 条）+ 最近 5 条待处理提醒区块。
pages/2*✅\_待办清单.py：顶部「🔔 截止提醒」区 + 每条已读/忽略按钮。
test_reminder.py（新）、docs/DATA-MODEL.md、README.md：验收测试与文档。
一个实现细节调整（相对计划）：幂等键最终为 UNIQUE(notice_id, tier, remind_on) 而非计划中的四列——SQLite 将 UNIQUE 列中的 NULL 视为互异，可空的 todo_id 会破坏无待办提醒的幂等性；因 notice_id 恒非空（通知路与兜底路都带），用它做键完全满足「同一天同一对象不重复」。

验收可直接执行：python scheduler.py --once（scheduler_log 会出现 reminder 成功记录），或造一条 deadline=今天+3 天的通知后跑扫描，首页即出现红点。

#### 模块 3.3 两步式交互 + 全链路演示（≈5h）

- 子任务：
  1. 按"两步式"改造涉及确认的交互（订阅保存、提醒忽略等：第一步预览/确认，第二步执行），绕开 Streamlit 长时状态问题
  2. 造数工具：脚本一键插入"未来截止 + 命中订阅词"的测试通知，供演示
  3. 全链路演示脚本/文档：发布 → 命中 → 提醒 → 待办 → 用户处理
- 涉及现有模块：`pages/`、`services/`、新建演示脚本（如 `tools/demo_reminder.py`）
- 依赖：模块 3.1、3.2
- 验收信号：按演示脚本执行 5 分钟可完整演示全链路；造数工具可重复执行不污染真实数据

模块 3.3 实施完成，所有验证通过。

交付内容
两步式交互（子任务 1）

ui/two*step.py（新）：request_action 存意图 → render_confirmation 第二步确认面板（预览 + 确认/取消 + flash 反馈），把长时写库操作与表单瞬态状态解耦。
services/subscription_service.py：新增 preview_subscription_matches()（纯只读，统计命中数 + 样例标题）。
pages/5*🔔*订阅管理.py：新增/编辑订阅 =「填词 → 预览预计命中 N/M 条 → 确认添加/保存」；重匹配全部通知 = 确认后执行。
pages/2*✅\_待办清单.py：提醒「忽略」=「点击 → 确认面板 → 确认忽略」；「已读」保持一步。
造数工具（子任务 2）

tools/seed_demo_data.py：插入 2 条「今天+3/+1 截止 + 命中订阅词」演示通知（source="演示数据" 隔离）、幂等创建「演示竞赛」订阅并回填命中、直插确定性待办；--clean 只清演示数据，URL/订阅双幂等，重复执行不污染真实数据。
全链路演示（子任务 3）

tools/demo_reminder.py --demo：发布 → 命中 → 提醒 → 待办 → 用户处理，内置幂等验证（重复扫描 created=0）、级联收敛验证（完成待办→提醒自动已读），PASS/FAIL 汇总 + 退出码。
docs/DEMO.md：5 分钟演示步骤 + UI 观察点 + 隔离/幂等说明；README 快速开始追加演示命令。
验证

test_demo.py（新）：A 预览只读 / B seed 幂等 / C 未来截止+命中 / D 提醒幂等 / E 用户处理级联 / F 清理隔离 — 全绿。
AppTest 实测两条 UI 流程：订阅两步式添加落库成功、待办页「忽略」两步确认后状态为 ignored。
回归 test_reminder.py、test_subscription.py 全绿；py_compile 全过。

**W3 周末验收**：M-闭环演示成功；幂等有自动化测试或脚本验证；演示不依赖手工改库。

---

### W4 单用户深度自用 + 埋点 + 终检（≈16h）

**周目标**：自己当真实用户用一周，所有行为留下数据；7 天自运行终检达标；产出 ≥3 条证据支持的下一步。

#### 模块 4.1 埋点（≈4h）

- 子任务：
  1. 新增事件表（事件类型、时间、关联对象 id、备注）
  2. 埋点事件集：页面访问、问答提问次数、待办生成点击、待办标记完成、**服务按钮点击（假按钮**，点后记录 + 弹"演示版"提示）
  3. 埋点写入为确定性代码，纯本地落库，不阻塞主流程
- 涉及现有模块：`storage/db.py`、`pages/`、`services/`
- 依赖：无
- 验收信号：自用两天后事件表有真实数据；假服务按钮点击数可查（门控 #1 数据）

模块 4.1 实施完成，test_events.py 全部通过（16 项离线 + AppTest 冒烟）。

改动清单

- storage/db.py：新增 events 事件表（event_type / ref_id / note / event_at，纯追加无 FK 无级联）+ insert_event / count_events_by_type / get_event_stats / get_recent_events；SCHEMA 自动建表，无需迁移。
- services/tracking_service.py（新）：track_event 每次自开独立短连接、整体 try/except 兜底——异常只记日志返回 False 绝不上抛（不阻塞主流程）+ 查询封装（count_events / get_event_stats / get_recent_events）+ 五类事件常量。
- 页面埋点接入：app.py + 6 个 pages 页面访问（session_state 按会话每页去重，rerun 不重复计）；通知浏览/待办清单「生成待办」→ todo_generate；待办清单「完成」→ todo_done；智能问答「发送」→ qa_ask（note=问题文本）。
- pages/6_服务市场.py（新，门控 #1 数据源）：3 个假服务卡片（打印/跑腿/文印），点「下单」→ 记录 service_button_click（ref_id=服务 key）→ st.toast「演示版：未接入真实下单服务」；页面自带点击统计查看。
- app.py 快捷入口：补「系统配置」「服务市场（演示）」第二行链接。
- test_events.py（新）：表结构 / 正常写入 / 五类事件 / **写入失败不阻塞主流程**（强制连接失败返回 False 不抛）/ 查询接口 / 服务市场页 AppTest 点「下单」后落库，16 项全过；6 页 + 服务市场页 AppTest 冒烟无异常。
- 文档：docs/DATA-MODEL.md 补 events 表与 ER；README 多页面清单补服务市场页。

验收对照

- 事件表有真实数据：五类事件全部落库（真库 data/notices.db 已自动建出 events 表，计数 0 待自用产生）。
- 假服务按钮点击数可查（门控 #1）：`python -c "import services.tracking_service as t; print(t.count_events(t.EVENT_SERVICE_CLICK))"`。
- 不阻塞主流程：track_event 全程 try/except，写库失败返回 False 不打断业务。

#### 模块 4.2 7 天自运行终检（≈6h）

- 子任务：
  1. 调度进程连续 7 天无人干预运行，每日例行检查：抓取成功率 ≥90%、提取失败率 <10%、token 消耗、异常日志
  2. 每周人工抽查 10 条抓取/提取结果（记录抽查表）
  3. 期间再做一次崩溃恢复演练（在真实运行中 kill 重启，验证续跑与不重复计费）
- 涉及现有模块：W1 调度器、计量表、`check_db.py` 扩展为"每日体检"脚本
- 依赖：W1 全部模块、W2 评测脚本（抽查可复用）
- 验收信号：M-自运行达标（连续 7 天 + 重启续跑 + 抓取成功率 ≥90%）；抽查表落盘

#### 模块 4.3 自用周报 + 下一步（≈6h）

- 子任务：
  1. 汇总自用数据：打开频率、问答次数、待办生成点击率、标记无用比例、订阅命中率、提醒处理率
  2. 对照阶段2 六门控点逐条填数据与结论（1 一键下单 / 2 管道稳定 / 3 批量 vs 按需 / 4 混合检索 / 5 模型分工 / 6 多用户）
  3. 写 ≥3 条"证据支持的下一步"；整理 `TROUBLESHOOTING.md`（记录 4 周遇到的最难 5 个 LLM/工程报错，含排查过程）
  4. 交付清单：本计划所有验收项 → 阶段2 输入物
- 涉及现有模块：汇总脚本（如 `tools/usage_report.py`）、文档
- 依赖：模块 4.1、4.2
- 验收信号：周报落盘；六门控每项有数据或明确"数据不足需补测"；TROUBLESHOOTING.md 有 5 个真实案例

**W4 周末验收**：M-自运行、M-检索线、M-闭环三项达成；阶段2 输入物齐备；门控 1/3/6 有信号。

---

## 3. 数据库演进总览（描述级）

现有：`notices` / `crawl_log` / `todos`。4 周新增 4 张表，全部本地 SQLite：

| 表           | 用途                       | 何时建 | 数据流向                       |
| ------------ | -------------------------- | ------ | ------------------------------ |
| token 计量表 | 每次 LLM 调用的 usage 账本 | W1-1.4 | utils/llm.py → 表 → 配置页展示 |
| 订阅表       | 订阅词/类型/启停           | W3-3.1 | 订阅管理页 → 表 → 规则引擎     |
| 提醒表       | 截止提醒记录与状态         | W3-3.2 | 扫描任务 → 表 → UI 红点        |
| 事件表       | 用户行为埋点               | W4-4.1 | 页面 → 表 → 周报汇总           |

约束：所有新增表沿用现有 `storage/db.py` 的建表与迁移方式（M2 已有迁移先例），不引入 ORM 以外的依赖。

## 4. 依赖关系

```
W1 主线（调度/指纹/游标/计量）────┐
W1 支线（Tool Calling，独立并行） ┤
W2（测试集→基线→过期→混合→污染） ┤  全部指向
W3（订阅→提醒→演示，依赖 W1 调度）┤  W4 终检与门控数据
W4（埋点/终检/周报）◄─────────────┘
```

- 关键依赖链：W1.1（调度器）是 W3.2（每日扫描）的前置；W2.2（基线）是 W2.3/W2.4 的前置；W2.3（过期策略）被 W3.2（过期判断）复用。
- 若某周主线延期：优先砍当周非依赖项（如 W1 支线、W2 的混合检索实验可以推迟到 W3 并行），不砍地基（调度、计量、测试集）。

## 5. 每周验收清单

- [ ] W1：调度进程 24h 无干预运行；崩溃重启演练通过（不重复计费）；计量表有数；Tool Calling 日志回放可演示
- [ ] W2：20 题测试集落盘；纯向量基线表产出；过期三档对比表产出；混合检索对比结论明确；污染专项可演示
- [ ] W3：全链路演示成功（发布→命中→提醒→处理）；提醒幂等验证；不依赖 Streamlit 在线
- [ ] W4：M-自运行达标；埋点数据齐全；自用周报 + 六门控数据 + TROUBLESHOOTING.md 落盘

## 6. 门控数据采集映射

| 门控（定稿）   | 数据来源                            | 采集周             | 判据                                    |
| -------------- | ----------------------------------- | ------------------ | --------------------------------------- |
| 1 一键下单     | 假服务按钮点击数                    | W4 埋点            | 自己无复购意愿/无人问"怎么真下单" → 砍  |
| 2 采集管道稳定 | 抓取/提取/重试/token 四类指标       | W1 计量 + W4 终检  | 抓取成功率 ≥90%、提取失败率 <10%        |
| 3 批量 vs 按需 | 待办生成点击次数 + 无用标记率       | W4 埋点            | 27 条里点开生成 <5 次 → 维持按需        |
| 4 混合检索     | W2 对比表                           | W2                 | MRR/Rec@k 提升 ≥10% 且无回归才上        |
| 5 模型分工     | 黄金集对比 + token 成本（issue #2） | 可在 W2 空档并行跑 | 便宜模型 ≥ 当前-5% 且成本减半 → 换      |
| 6 多用户       | 单用户使用数据                      | W4 周报            | 无明确部署场景 → 阶段2 多用户为虚构需求 |
| 7 APP 形态     | 阶段2 试装反馈                      | 阶段2              | WebView 壳体验差再评估原生              |

## 7. 学习产出物清单

| 周  | 产出物                                       | 证明学到什么                            |
| --- | -------------------------------------------- | --------------------------------------- |
| W1  | 调度运行日志回放 + token 计量截图            | 幂等、断点、可观测是 AI 系统的通用能力  |
| W2  | 20 题测试集 + 三张对比表 + 污染策略文档      | RAG 三层评估方法论、错误归因            |
| W3  | 全链路演示脚本 + 造数工具                    | 规则引擎与定时扫描模式                  |
| W4  | TROUBLESHOOTING.md（5 个最难报错）+ 自用周报 | 调试 Agent 的硬功夫；数据说话的决策习惯 |

## 8. 风险应急映射

| 定稿风险                                     | 发生周    | 应急动作                                                                                    |
| -------------------------------------------- | --------- | ------------------------------------------------------------------------------------------- |
| 文档工程陷阱（文档行数 > 代码行数连续 2 周） | 全程      | 本计划只维护"验收清单勾选 + 新增产出物"，不扩展文档体系；新想法进 `docs/待确认.md` 或 issue |
| 工具循环死锁                                 | W1 支线   | 模块 1.5 已内置 tool_call_id 去重 + 输出缓存；出现死锁以日志回放为准排查                    |
| RAG 污染                                     | W2        | 模块 2.5 的一致性校验脚本成为常驻工具，每次删除/重建后必跑                                  |
| 成本无声失控                                 | W1 起全程 | 计量表周检；若提取失败率升高导致重试放大，先查 2.5 的污染与 3.2 的幂等，再调 prompt         |
| 评估靠感觉                                   | W2 起     | 测试集常驻，W4 终检期间每周末跑一遍对比表，防止回归                                         |

---

## 附：4 周小时预算汇总

| 周  | 主线           | 支线      | 合计 |
| --- | -------------- | --------- | ---- |
| W1  | 14h（1.1-1.4） | 4h（1.5） | 18h  |
| W2  | 17h（2.1-2.5） | —         | 17h  |
| W3  | 17h（3.1-3.3） | —         | 17h  |
| W4  | 16h（4.1-4.3） | —         | 16h  |

> 若某周实际投入不足：按 依赖顺序 砍模块（先保 1.1、1.4、2.1、2.2、3.2、4.2），砍掉的模块记录到 W4 周报的"证据支持的下一步"，作为阶段2 输入而非丢失。
